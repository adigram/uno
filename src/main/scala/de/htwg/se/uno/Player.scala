package de.htwg

class Player( val hand:List[(Int,Int)] , val number:Int){
    // def printHand(): String= "Hand Player "  + player +"\n" + hand.map(k=> colour.apply(k._1) +" "+symbol.apply(k._2)).mkString("  |  ")  + "\n"
}